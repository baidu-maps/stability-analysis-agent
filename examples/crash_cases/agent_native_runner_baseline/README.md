# Native baseline and post-fix replay

This case has no frontend. The profile declares the native replay command; generated binaries stay outside the repository. Static CI only validates the contract.
